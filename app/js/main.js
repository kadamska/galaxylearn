$(document).ready(function(){
	/*
	// Update the Live Preview of the Title
	$("form[name=StoryEdit] input[name=title]").live('keyup',function () {
		var value = $(this).val();
		$(".title-preview").html(value);
	}).keyup();	

	// Update the Live Preview of the Content
	$("form[name=StoryEdit] textarea[name=body]").live('keyup',function () {
		var value = $(this).val().replace(/\r\n|\r|\n/g,"<br />");
		$(".content-preview").html(value);
	}).keyup();		

	*/
});

